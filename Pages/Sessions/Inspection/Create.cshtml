@page "/Sessions/{id:guid}/Inspection/Create"
@model Gixat.Web.Pages.Sessions.Inspection.CreateModel
@using Gixat.Web.Modules.Sessions.Enums
@{
    ViewData["Title"] = "Start Inspection";
    Layout = "_AppLayout";
}

<div class="max-w-3xl mx-auto">
    <!-- Back Navigation -->
    <a href="/Sessions/Details/@Model.Session?.Id" class="inline-flex items-center gap-2 text-apple-gray hover:text-apple-blue no-underline mb-6 text-sm transition-colors animate-fade-in">
        <i class="bi bi-arrow-left"></i>
        Back to Session
    </a>

    <!-- Header Card -->
    <div class="card-apple overflow-hidden mb-6 animate-slide-up">
        <div class="bg-gradient-to-r from-apple-teal to-apple-blue p-6 text-white">
            <div class="flex items-center gap-4">
                <div class="w-12 h-12 rounded-xl bg-white/20 backdrop-blur flex items-center justify-center">
                    <i class="bi bi-search text-xl"></i>
                </div>
                <div>
                    <h1 class="text-xl font-semibold tracking-tight">Start Inspection</h1>
                    <p class="text-sm opacity-90">Session #@Model.Session?.SessionNumber • @Model.Session?.VehicleDisplayName</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Form Card -->
    <div class="card-apple p-6 animate-slide-up" style="animation-delay: 0.1s;">
        <form method="post" id="inspectionForm">
            <div class="space-y-5">
                <!-- Title -->
                <div>
                    <label asp-for="Input.Title" class="block text-sm font-medium text-apple-dark mb-1.5">Inspection Title <span class="text-apple-red">*</span></label>
                    <input asp-for="Input.Title" class="input-apple-outlined" placeholder="e.g., Full Vehicle Inspection" required />
                    <span asp-validation-for="Input.Title" class="text-apple-red text-xs mt-1"></span>
                </div>

                <!-- Priority -->
                <div>
                    <label asp-for="Input.OverallPriority" class="block text-sm font-medium text-apple-dark mb-3">Priority</label>
                    <div class="flex flex-wrap gap-3">
                        <label class="priority-option">
                            <input type="radio" name="Input.OverallPriority" value="@Priority.Low" class="hidden peer" />
                            <div class="px-4 py-2 border border-gray-200 rounded-xl cursor-pointer text-sm font-medium text-apple-gray peer-checked:border-apple-green peer-checked:bg-apple-green/10 peer-checked:text-apple-green transition-all">
                                <i class="bi bi-arrow-down-short"></i> Low
                            </div>
                        </label>
                        <label class="priority-option">
                            <input type="radio" name="Input.OverallPriority" value="@Priority.Normal" class="hidden peer" checked />
                            <div class="px-4 py-2 border border-gray-200 rounded-xl cursor-pointer text-sm font-medium text-apple-gray peer-checked:border-apple-blue peer-checked:bg-apple-blue/10 peer-checked:text-apple-blue transition-all">
                                <i class="bi bi-dash"></i> Normal
                            </div>
                        </label>
                        <label class="priority-option">
                            <input type="radio" name="Input.OverallPriority" value="@Priority.High" class="hidden peer" />
                            <div class="px-4 py-2 border border-gray-200 rounded-xl cursor-pointer text-sm font-medium text-apple-gray peer-checked:border-apple-orange peer-checked:bg-apple-orange/10 peer-checked:text-apple-orange transition-all">
                                <i class="bi bi-arrow-up-short"></i> High
                            </div>
                        </label>
                        <label class="priority-option">
                            <input type="radio" name="Input.OverallPriority" value="@Priority.Urgent" class="hidden peer" />
                            <div class="px-4 py-2 border border-gray-200 rounded-xl cursor-pointer text-sm font-medium text-apple-gray peer-checked:border-apple-red peer-checked:bg-apple-red/10 peer-checked:text-apple-red transition-all">
                                <i class="bi bi-exclamation-triangle"></i> Urgent
                            </div>
                        </label>
                    </div>
                </div>

                <!-- Description -->
                <div>
                    <label asp-for="Input.Description" class="block text-sm font-medium text-apple-dark mb-1.5">Description</label>
                    <textarea asp-for="Input.Description" rows="3" class="input-apple-outlined resize-none" placeholder="Describe the scope of the inspection..."></textarea>
                </div>

                <!-- Media Upload Section -->
                <div>
                    <label class="block text-sm font-medium text-apple-dark mb-3">Photos & Videos</label>
                    <div id="dropZone" class="border-2 border-dashed border-gray-200 rounded-2xl p-8 text-center hover:border-apple-blue hover:bg-apple-blue/5 transition-all cursor-pointer group">
                        <input type="file" id="mediaUpload" class="hidden" accept="image/*,video/*" multiple />
                        <div class="w-14 h-14 rounded-2xl bg-gray-100 group-hover:bg-apple-blue/10 flex items-center justify-center mx-auto mb-4 transition-colors">
                            <i class="bi bi-cloud-arrow-up text-2xl text-apple-gray group-hover:text-apple-blue transition-colors"></i>
                        </div>
                        <p class="text-sm font-medium text-apple-dark mb-1">Drop photos or videos here</p>
                        <p class="text-xs text-apple-gray">or click to browse • Images will be compressed automatically</p>
                    </div>

                    <div id="uploadProgress" class="hidden mt-4">
                        <div class="flex items-center gap-3 p-4 bg-apple-blue/10 rounded-xl">
                            <div class="animate-spin rounded-full h-5 w-5 border-2 border-apple-blue border-t-transparent"></div>
                            <span class="text-sm font-medium text-apple-blue">Uploading and compressing...</span>
                        </div>
                    </div>

                    <div id="mediaGrid" class="grid grid-cols-3 md:grid-cols-4 gap-3 mt-4"></div>
                    <input type="hidden" asp-for="Input.MediaItemIds" id="mediaItemIds" />
                </div>

                <!-- Info Box -->
                <div class="bg-apple-blue/10 border border-apple-blue/20 rounded-xl p-4">
                    <div class="flex gap-3">
                        <i class="bi bi-info-circle text-apple-blue text-lg flex-shrink-0"></i>
                        <div>
                            <h4 class="text-sm font-medium text-apple-dark">What happens next?</h4>
                            <p class="text-sm text-apple-gray mt-1">After creating the inspection, you'll be able to add inspection items, record conditions, and upload more photos.</p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Actions -->
            <div class="flex justify-end gap-3 mt-8 pt-6 border-t border-gray-100">
                <a href="/Sessions/Details/@Model.Session?.Id" class="btn-apple-secondary">Cancel</a>
                <button type="submit" class="btn-apple-primary bg-gradient-to-r from-apple-teal to-apple-blue">
                    <i class="bi bi-search"></i>Start Inspection
                </button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
<script>
    const sessionId = '@Model.Session?.Id';
    const mediaGrid = document.getElementById('mediaGrid');
    const uploadProgress = document.getElementById('uploadProgress');
    const dropZone = document.getElementById('dropZone');
    const mediaItemIds = document.getElementById('mediaItemIds');
    const uploadedIds = [];

    dropZone.addEventListener('click', () => document.getElementById('mediaUpload').click());

    dropZone.addEventListener('dragover', (e) => {
        e.preventDefault();
        dropZone.classList.add('border-apple-blue', 'bg-apple-blue/10');
    });
    dropZone.addEventListener('dragleave', () => {
        dropZone.classList.remove('border-apple-blue', 'bg-apple-blue/10');
    });
    dropZone.addEventListener('drop', (e) => {
        e.preventDefault();
        dropZone.classList.remove('border-apple-blue', 'bg-apple-blue/10');
        const files = Array.from(e.dataTransfer.files).filter(f => f.type.startsWith('image/') || f.type.startsWith('video/'));
        if (files.length > 0) handleFiles(files);
    });

    document.getElementById('mediaUpload').addEventListener('change', function(e) {
        const files = Array.from(e.target.files);
        if (files.length > 0) handleFiles(files);
        e.target.value = '';
    });

    async function handleFiles(files) {
        uploadProgress.classList.remove('hidden');

        for (const file of files) {
            try {
                let processedFile = file;
                if (file.type.startsWith('image/')) {
                    processedFile = await compressImage(file);
                }

                const formData = new FormData();
                formData.append('file', processedFile);

                const response = await fetch(`/Sessions/${sessionId}/Inspection/Create?handler=Upload`, {
                    method: 'POST',
                    body: formData,
                    headers: {
                        'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]')?.value || ''
                    }
                });

                const result = await response.json();
                if (!result.success) continue;

                await fetch(result.uploadUrl, {
                    method: 'PUT',
                    body: processedFile,
                    headers: { 'Content-Type': processedFile.type }
                });

                const confirmResponse = await fetch(`/Sessions/${sessionId}/Inspection/Create?handler=ConfirmUpload&mediaItemId=${result.mediaItemId}`, {
                    method: 'POST',
                    headers: {
                        'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]')?.value || ''
                    }
                });

                const confirmResult = await confirmResponse.json();
                if (confirmResult.success) {
                    uploadedIds.push(confirmResult.media.id);
                    mediaItemIds.value = uploadedIds.join(',');
                    addMediaToGrid(confirmResult.media);
                }
            } catch (error) {
                console.error('Upload error:', error);
            }
        }

        uploadProgress.classList.add('hidden');
    }

    async function compressImage(file) {
        return new Promise((resolve) => {
            const reader = new FileReader();
            reader.onload = function(e) {
                const img = new Image();
                img.onload = function() {
                    const canvas = document.createElement('canvas');
                    const maxSize = 1920;
                    let width = img.width, height = img.height;

                    if (width > height && width > maxSize) {
                        height = (height * maxSize) / width;
                        width = maxSize;
                    } else if (height > maxSize) {
                        width = (width * maxSize) / height;
                        height = maxSize;
                    }

                    canvas.width = width;
                    canvas.height = height;
                    canvas.getContext('2d').drawImage(img, 0, 0, width, height);

                    canvas.toBlob((blob) => {
                        resolve(new File([blob], file.name, { type: 'image/jpeg' }));
                    }, 'image/jpeg', 0.8);
                };
                img.src = e.target.result;
            };
            reader.readAsDataURL(file);
        });
    }

    function addMediaToGrid(media) {
        const div = document.createElement('div');
        div.className = 'relative group rounded-xl overflow-hidden';
        div.dataset.mediaId = media.id;

        if (media.mediaType === 0) {
            div.innerHTML = `
                <img src="${media.s3Url}" class="w-full h-24 object-cover" />
                <button type="button" onclick="deleteMedia('${media.id}')" 
                        class="absolute top-2 right-2 w-6 h-6 bg-apple-red text-white rounded-full flex items-center justify-center opacity-0 group-hover:opacity-100 transition-all shadow-lg">
                    <i class="bi bi-x text-sm"></i>
                </button>`;
        } else {
            div.innerHTML = `
                <div class="w-full h-24 bg-gray-100 flex items-center justify-center">
                    <i class="bi bi-play-circle text-2xl text-apple-gray"></i>
                </div>
                <button type="button" onclick="deleteMedia('${media.id}')" 
                        class="absolute top-2 right-2 w-6 h-6 bg-apple-red text-white rounded-full flex items-center justify-center opacity-0 group-hover:opacity-100 transition-all shadow-lg">
                    <i class="bi bi-x text-sm"></i>
                </button>`;
        }
        mediaGrid.appendChild(div);
    }

    async function deleteMedia(mediaId) {
        const response = await fetch(`/Sessions/${sessionId}/Inspection/Create?handler=DeleteMedia&mediaId=${mediaId}`, {
            method: 'POST',
            headers: { 'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]')?.value || '' }
        });
        const result = await response.json();
        if (result.success) {
            document.querySelector(`[data-media-id="${mediaId}"]`)?.remove();
            const idx = uploadedIds.indexOf(mediaId);
            if (idx > -1) uploadedIds.splice(idx, 1);
            mediaItemIds.value = uploadedIds.join(',');
        }
    }
</script>
<form method="post" class="hidden">@Html.AntiForgeryToken()</form>
}

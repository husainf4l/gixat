@{
    Layout = "_Layout";
    var currentPath = Context.Request.Path.ToString().ToLower();
}

@RenderSection("Styles", required: false)

<!-- Top Header Bar -->
<header class="fixed top-0 left-0 right-0 z-50 h-14 bg-white/80 backdrop-blur-md border-b border-slate-200">
    <div class="h-full px-4 lg:px-6 flex items-center justify-between">
        <!-- Left: Logo & Mobile Menu -->
        <div class="flex items-center gap-3">
            <button id="mobile-menu-btn" class="lg:hidden p-2 -ml-2 text-slate-600 hover:text-slate-900 hover:bg-slate-100 rounded-lg transition-all duration-150">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
                </svg>
            </button>
            <a href="/Dashboard" class="flex items-center gap-2">
                <img src="/avatars/gixat-logo(3).png" alt="Gixat" class="h-8 w-auto">
            </a>
        </div>

        <!-- Center: Search (Desktop) -->
        <div class="hidden md:flex flex-1 max-w-md mx-8">
            <div class="relative w-full" id="searchContainer">
                <svg class="w-4 h-4 absolute left-3 top-1/2 -translate-y-1/2 text-slate-400 pointer-events-none z-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
                </svg>
                <input type="text" id="globalSearch" placeholder="Search sessions, clients, vehicles..." 
                       class="w-full pl-9 pr-4 py-1.5 bg-slate-100 border-0 rounded-lg text-[13px] placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-primary-600/30 focus:bg-white transition-all" 
                       autocomplete="off" />
                
                <!-- Search Results Dropdown -->
                <div id="searchResults" class="hidden absolute top-full left-0 right-0 mt-2 bg-white rounded-xl shadow-2xl border border-slate-200 overflow-hidden max-h-[400px] overflow-y-auto">
                    <!-- Results will be populated here -->
                </div>
            </div>
        </div>

        <!-- Right: Actions -->
        <div class="flex items-center gap-1">
            <!-- Notifications -->
            <button class="relative p-2 text-slate-600 hover:text-slate-900 hover:bg-slate-100 rounded-lg transition-all duration-150">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"/>
                </svg>
                <span class="absolute top-1.5 right-1.5 w-2 h-2 bg-red-500 rounded-full"></span>
            </button>
            
            <!-- User Menu -->
            <div class="flex items-center gap-2 pl-2 ml-2 border-l border-slate-200">
                <div class="w-7 h-7 rounded-full bg-gradient-to-br from-primary-500 to-blue-600 flex items-center justify-center text-white text-[11px] font-medium">
                    @(User.Identity?.Name?.Substring(0, 2).ToUpper() ?? "U")
                </div>
                <span class="hidden sm:block text-[13px] font-medium text-slate-900 max-w-[100px] truncate">
                    @User.Identity?.Name
                </span>
                <a href="/Auth/Logout" class="p-1.5 text-slate-600 hover:text-red-600 hover:bg-red-50 rounded-lg transition-all duration-150" title="Logout">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
                    </svg>
                </a>
            </div>
        </div>
    </div>
</header>

<!-- Mobile Sidebar Overlay -->
<div id="mobile-backdrop" class="fixed inset-0 bg-black/30 backdrop-blur-sm z-40 opacity-0 pointer-events-none transition-opacity duration-200 lg:hidden"></div>

<!-- Mobile Sidebar -->
<aside id="mobile-sidebar" class="fixed inset-y-0 left-0 w-64 bg-white z-50 transform -translate-x-full transition-transform duration-200 lg:hidden shadow-xl">
    <div class="flex items-center justify-between h-14 px-4 border-b border-slate-200">
        <span class="text-[13px] font-semibold text-slate-900">Menu</span>
        <button id="close-mobile-menu" class="p-2 text-slate-600 hover:text-slate-900 hover:bg-slate-100 rounded-lg transition-all duration-150">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
            </svg>
        </button>
    </div>
    
    <!-- Mobile Search -->
    <div class="p-3 border-b border-slate-100">
        <div class="relative">
            <svg class="w-4 h-4 absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
            </svg>
            <input type="text" placeholder="Search..." class="w-full pl-9 pr-3 py-2 bg-slate-100 border-0 rounded-lg text-[13px] focus:outline-none focus:ring-2 focus:ring-primary-600/30" />
        </div>
    </div>
    
    <nav class="p-3 overflow-y-auto h-[calc(100%-122px)]">
        <p class="px-2 pb-2 text-[10px] font-semibold text-slate-500 uppercase tracking-wider">Main</p>
        <div class="space-y-0.5">
            <a href="/Dashboard" class="flex items-center gap-2.5 px-2.5 py-2 rounded-lg text-[13px] font-medium transition-all duration-150 @(currentPath == "/dashboard" ? "bg-primary-600 text-white" : "text-slate-700 hover:bg-slate-100")">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 5a1 1 0 011-1h4a1 1 0 011 1v7a1 1 0 01-1 1H5a1 1 0 01-1-1V5zM14 5a1 1 0 011-1h4a1 1 0 011 1v3a1 1 0 01-1 1h-4a1 1 0 01-1-1V5zM4 16a1 1 0 011-1h4a1 1 0 011 1v3a1 1 0 01-1 1H5a1 1 0 01-1-1v-3zM14 12a1 1 0 011-1h4a1 1 0 011 1v7a1 1 0 01-1 1h-4a1 1 0 01-1-1v-7z"/>
                </svg>
                Dashboard
            </a>
            <a href="/Sessions" class="flex items-center gap-2.5 px-2.5 py-2 rounded-lg text-[13px] font-medium transition-all duration-150 @(currentPath.StartsWith("/sessions") ? "bg-primary-600 text-white" : "text-slate-700 hover:bg-slate-100")">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"/>
                </svg>
                Sessions
            </a>
            <a href="/Clients" class="flex items-center gap-2.5 px-2.5 py-2 rounded-lg text-[13px] font-medium transition-all duration-150 @(currentPath.StartsWith("/clients") ? "bg-primary-600 text-white" : "text-slate-700 hover:bg-slate-100")">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"/>
                </svg>
                Clients
            </a>
            <a href="/Appointments" class="flex items-center gap-2.5 px-2.5 py-2 rounded-lg text-[13px] font-medium transition-all duration-150 @(currentPath.StartsWith("/appointments") ? "bg-primary-600 text-white" : "text-slate-700 hover:bg-slate-100")">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                </svg>
                Appointments
            </a>
            <a href="/JobCards" class="flex items-center gap-2.5 px-2.5 py-2 rounded-lg text-[13px] font-medium transition-all duration-150 @(currentPath.StartsWith("/jobcards") ? "bg-primary-600 text-white" : "text-slate-700 hover:bg-slate-100")">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
                </svg>
                Job Cards
            </a>
        </div>
        
        <p class="px-2 pt-5 pb-2 text-[10px] font-semibold text-slate-500 uppercase tracking-wider">Business</p>
        <div class="space-y-0.5">
            <a href="/Invoices" class="flex items-center gap-2.5 px-2.5 py-2 rounded-lg text-[13px] font-medium transition-all duration-150 @(currentPath.StartsWith("/invoices") ? "bg-primary-600 text-white" : "text-slate-700 hover:bg-slate-100")">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 14l6-6m-5.5.5h.01m4.99 5h.01M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16l3.5-2 3.5 2 3.5-2 3.5 2z"/>
                </svg>
                Invoices
            </a>
            <a href="/Inventory" class="flex items-center gap-2.5 px-2.5 py-2 rounded-lg text-[13px] font-medium transition-all duration-150 @(currentPath.StartsWith("/inventory") ? "bg-primary-600 text-white" : "text-slate-700 hover:bg-slate-100")">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"/>
                </svg>
                Inventory
            </a>
        </div>
        
        <p class="px-2 pt-5 pb-2 text-[10px] font-semibold text-slate-500 uppercase tracking-wider">Settings</p>
        <div class="space-y-0.5">
            <a href="/Settings/Users" class="flex items-center gap-2.5 px-2.5 py-2 rounded-lg text-[13px] font-medium transition-all duration-150 @(currentPath.StartsWith("/settings") ? "bg-primary-600 text-white" : "text-slate-700 hover:bg-slate-100")">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/>
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
                </svg>
                Settings
            </a>
        </div>
    </nav>
</aside>

<div class="flex pt-14 min-h-screen bg-slate-50">
    <!-- Desktop Sidebar -->
    <aside class="hidden lg:flex lg:flex-col w-56 bg-white border-r border-slate-200 fixed h-[calc(100vh-56px)] overflow-y-auto">
        <nav class="flex-1 p-3">
            <p class="px-2 pb-2 text-[10px] font-semibold text-slate-500 uppercase tracking-wider">Main</p>
            <div class="space-y-0.5">
                <a href="/Dashboard" class="flex items-center gap-2.5 px-2.5 py-2 rounded-lg text-[13px] font-medium transition-all duration-150 @(currentPath == "/dashboard" ? "bg-primary-600 text-white" : "text-slate-700 hover:bg-slate-100")">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 5a1 1 0 011-1h4a1 1 0 011 1v7a1 1 0 01-1 1H5a1 1 0 01-1-1V5zM14 5a1 1 0 011-1h4a1 1 0 011 1v3a1 1 0 01-1 1h-4a1 1 0 01-1-1V5zM4 16a1 1 0 011-1h4a1 1 0 011 1v3a1 1 0 01-1 1H5a1 1 0 01-1-1v-3zM14 12a1 1 0 011-1h4a1 1 0 011 1v7a1 1 0 01-1 1h-4a1 1 0 01-1-1v-7z"/>
                    </svg>
                    Dashboard
                </a>
                <a href="/Sessions" class="flex items-center gap-2.5 px-2.5 py-2 rounded-lg text-[13px] font-medium transition-all duration-150 @(currentPath.StartsWith("/sessions") ? "bg-primary-600 text-white" : "text-slate-700 hover:bg-slate-100")">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"/>
                    </svg>
                    Sessions
                </a>
                <a href="/Clients" class="flex items-center gap-2.5 px-2.5 py-2 rounded-lg text-[13px] font-medium transition-all duration-150 @(currentPath.StartsWith("/clients") ? "bg-primary-600 text-white" : "text-slate-700 hover:bg-slate-100")">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"/>
                    </svg>
                    Clients
                </a>
                <a href="/Appointments" class="flex items-center gap-2.5 px-2.5 py-2 rounded-lg text-[13px] font-medium transition-all duration-150 @(currentPath.StartsWith("/appointments") ? "bg-primary-600 text-white" : "text-slate-700 hover:bg-slate-100")">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                    </svg>
                    Appointments
                </a>
                <a href="/JobCards" class="flex items-center gap-2.5 px-2.5 py-2 rounded-lg text-[13px] font-medium transition-all duration-150 @(currentPath.StartsWith("/jobcards") ? "bg-primary-600 text-white" : "text-slate-700 hover:bg-slate-100")">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
                    </svg>
                    Job Cards
                </a>
            </div>
            
            <p class="px-2 pt-5 pb-2 text-[10px] font-semibold text-slate-500 uppercase tracking-wider">Business</p>
            <div class="space-y-0.5">
                <a href="/Invoices" class="flex items-center gap-2.5 px-2.5 py-2 rounded-lg text-[13px] font-medium transition-all duration-150 @(currentPath.StartsWith("/invoices") ? "bg-primary-600 text-white" : "text-slate-700 hover:bg-slate-100")">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 14l6-6m-5.5.5h.01m4.99 5h.01M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16l3.5-2 3.5 2 3.5-2 3.5 2z"/>
                    </svg>
                    Invoices
                </a>
                <a href="/Inventory" class="flex items-center gap-2.5 px-2.5 py-2 rounded-lg text-[13px] font-medium transition-all duration-150 @(currentPath.StartsWith("/inventory") ? "bg-primary-600 text-white" : "text-slate-700 hover:bg-slate-100")">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"/>
                    </svg>
                    Inventory
                </a>
            </div>
            
            <p class="px-2 pt-5 pb-2 text-[10px] font-semibold text-slate-500 uppercase tracking-wider">Settings</p>
            <div class="space-y-0.5">
                <a href="/Settings/Users" class="flex items-center gap-2.5 px-2.5 py-2 rounded-lg text-[13px] font-medium transition-all duration-150 @(currentPath.StartsWith("/settings") ? "bg-primary-600 text-white" : "text-slate-700 hover:bg-slate-100")">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/>
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
                    </svg>
                    Settings
                </a>
            </div>
        </nav>
    </aside>

    <!-- Main Content -->
    <main class="flex-1 lg:ml-56">
        <div class="p-4 lg:p-6 max-w-7xl mx-auto">
            @RenderBody()
        </div>
    </main>
</div>

@RenderSection("Scripts", required: false)

<script>
    document.addEventListener('DOMContentLoaded', function() {
        const mobileMenuBtn = document.getElementById('mobile-menu-btn');
        const mobileSidebar = document.getElementById('mobile-sidebar');
        const mobileBackdrop = document.getElementById('mobile-backdrop');
        const closeMobileMenuBtn = document.getElementById('close-mobile-menu');

        function openMenu() {
            mobileSidebar.classList.remove('-translate-x-full');
            mobileBackdrop.classList.remove('opacity-0', 'pointer-events-none');
            document.body.style.overflow = 'hidden';
        }

        function closeMenu() {
            mobileSidebar.classList.add('-translate-x-full');
            mobileBackdrop.classList.add('opacity-0', 'pointer-events-none');
            document.body.style.overflow = '';
        }

        if (mobileMenuBtn) mobileMenuBtn.addEventListener('click', openMenu);
        if (mobileBackdrop) mobileBackdrop.addEventListener('click', closeMenu);
        if (closeMobileMenuBtn) closeMobileMenuBtn.addEventListener('click', closeMenu);
        
        // Close on escape key
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape') closeMenu();
        });
        
        // Global Search Functionality
        const searchInput = document.getElementById('globalSearch');
        const searchResults = document.getElementById('searchResults');
        let searchTimeout;
        
        if (searchInput && searchResults) {
            searchInput.addEventListener('input', function(e) {
                const query = e.target.value.trim();
                
                clearTimeout(searchTimeout);
                
                if (query.length < 2) {
                    searchResults.classList.add('hidden');
                    return;
                }
                
                searchTimeout = setTimeout(() => performSearch(query), 300);
            });
            
            searchInput.addEventListener('focus', function(e) {
                if (e.target.value.trim().length >= 2) {
                    performSearch(e.target.value.trim());
                }
            });
            
            // Close dropdown when clicking outside
            document.addEventListener('click', function(e) {
                if (!e.target.closest('#searchContainer')) {
                    searchResults.classList.add('hidden');
                }
            });
        }
        
        async function performSearch(query) {
            try {
                const response = await fetch(`/api/search?q=${encodeURIComponent(query)}`);
                const data = await response.json();
                displayResults(data);
            } catch (error) {
                console.error('Search error:', error);
                displayResults({ sessions: [], clients: [], vehicles: [] });
            }
        }
        
        function displayResults(data) {
            const results = searchResults;
            const hasResults = (data.sessions?.length > 0) || (data.clients?.length > 0) || (data.vehicles?.length > 0);
            
            if (!hasResults) {
                results.innerHTML = `
                    <div class="p-8 text-center">
                        <svg class="w-12 h-12 mx-auto text-slate-300 mb-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
                        </svg>
                        <p class="text-sm text-slate-600">No results found</p>
                    </div>
                `;
                results.classList.remove('hidden');
                return;
            }
            
            let html = '<div class="py-2">';
            
            // Sessions
            if (data.sessions?.length > 0) {
                html += `
                    <div class="px-3 py-2">
                        <div class="text-[11px] font-semibold text-slate-400 uppercase tracking-wider">Sessions</div>
                    </div>
                `;
                data.sessions.forEach(session => {
                    const statusColor = getStatusColor(session.status);
                    html += `
                        <a href="/Sessions/Details/${session.id}" class="flex items-center gap-3 px-3 py-2.5 hover:bg-slate-50 transition-colors group">
                            <div class="w-8 h-8 rounded-lg bg-primary-100 flex items-center justify-center flex-shrink-0">
                                <svg class="w-4 h-4 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"/>
                                </svg>
                            </div>
                            <div class="flex-1 min-w-0">
                                <div class="flex items-center gap-2">
                                    <span class="text-sm font-medium text-slate-900">#${session.sessionNumber}</span>
                                    <span class="inline-flex items-center gap-1 px-2 py-0.5 text-[10px] font-medium rounded-full ${statusColor}">
                                        ${session.status}
                                    </span>
                                </div>
                                <div class="text-xs text-slate-600 truncate">${session.clientName} • ${session.vehicleName}</div>
                            </div>
                        </a>
                    `;
                });
            }
            
            // Clients
            if (data.clients?.length > 0) {
                html += `
                    <div class="px-3 py-2 mt-2 border-t border-slate-100">
                        <div class="text-[11px] font-semibold text-slate-400 uppercase tracking-wider">Clients</div>
                    </div>
                `;
                data.clients.forEach(client => {
                    html += `
                        <a href="/Clients/${client.id}" class="flex items-center gap-3 px-3 py-2.5 hover:bg-slate-50 transition-colors group">
                            <div class="w-8 h-8 rounded-full bg-gradient-to-br from-primary-500 to-primary-600 flex items-center justify-center text-white text-xs font-medium flex-shrink-0">
                                ${client.initials}
                            </div>
                            <div class="flex-1 min-w-0">
                                <div class="text-sm font-medium text-slate-900">${client.name}</div>
                                <div class="text-xs text-slate-600 truncate">${client.phone}${client.email ? ' • ' + client.email : ''}</div>
                            </div>
                        </a>
                    `;
                });
            }
            
            // Vehicles
            if (data.vehicles?.length > 0) {
                html += `
                    <div class="px-3 py-2 mt-2 border-t border-slate-100">
                        <div class="text-[11px] font-semibold text-slate-400 uppercase tracking-wider">Vehicles</div>
                    </div>
                `;
                data.vehicles.forEach(vehicle => {
                    html += `
                        <a href="/Clients/${vehicle.clientId}" class="flex items-center gap-3 px-3 py-2.5 hover:bg-slate-50 transition-colors group">
                            <div class="w-8 h-8 rounded-lg bg-orange-100 flex items-center justify-center flex-shrink-0">
                                <svg class="w-4 h-4 text-orange-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
                                </svg>
                            </div>
                            <div class="flex-1 min-w-0">
                                <div class="text-sm font-medium text-slate-900">${vehicle.make} ${vehicle.model}</div>
                                <div class="text-xs text-slate-600 truncate">${vehicle.licensePlate} • ${vehicle.clientName}</div>
                            </div>
                        </a>
                    `;
                });
            }
            
            html += '</div>';
            results.innerHTML = html;
            results.classList.remove('hidden');
        }
        
        function getStatusColor(status) {
            const colors = {
                'CheckedIn': 'bg-cyan-100 text-cyan-700',
                'InProgress': 'bg-primary-100 text-primary-700',
                'Completed': 'bg-green-100 text-green-700',
                'Closed': 'bg-slate-100 text-slate-700',
                'Cancelled': 'bg-red-100 text-red-700'
            };
            return colors[status] || 'bg-slate-100 text-slate-700';
        }
    });
</script>
